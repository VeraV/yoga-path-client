export interface Limitation {
  id: number;
  name: string;
  description: string;
  notes: string;
}

export interface YogaStyle {
  id: number;
  name: string;
  description: string;
  notes: string;
}
